# Reproducible local/CI quality environment (Python 3.11).
# Runtime images keep their own minimal dependency manifests.
-r prism/requirements.txt
-r src/api/requirements.txt
-r src/generator/requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
httpx2>=2.0,<3.0
matplotlib>=3.8,<4.0
ruff==0.12.7
PyYAML>=6.0,<7.0
