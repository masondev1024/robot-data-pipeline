__pycache__/
.venv/
*.pyc
*.pyo
node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# phase execution outputs (어느 위치든)
**/phases/**/phase*-output.json
**/phases/**/step*-output.json

# 환경변수 — 절대 커밋 금지
.env
.env.local
.env.*.local
.env.example
!.env.example
# Terraform 상태 및 자격증명
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfstate.*.backup
terraform/*.tfstate.*
terraform/*.tfvars
!terraform/example.tfvars
terraform/.terraform.lock.hcl
# tfvars 추가 패턴 (어느 위치든 보호)
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json

# Lambda 빌드 아티팩트
terraform/lambda_*.zip

# Flink Application 모드 산출물 — Studio Notebook 채택(ADR-010 REVISED 2026-04-30) 후 deprecated
flink/anomaly_detection.zip
flink/lib/*.jar
flink/*_deploy.log

# Seed 데이터 — 전체 Kaggle 데이터셋은 커밋 금지 (sample만 허용)
data/seed_data.csv
data/*.zip
data/*.duckdb

# 시연 녹화 영상 — 용량 커서 git LFS 없이는 절대 commit 금지
prism/recordings/
# prism 배포 빌드 아티팩트(zip)는 추적 X — 소스에서 재생성
prism/*.zip
*.mov
*.mp4

# legacy/ 안의 terraform state 도 동일 보호 (credential leak 방지)
**/.terraform/
**/*.tfstate
**/*.tfstate.backup
**/*.tfstate.*
**/*.tfstate.*.backup
**/.terraform.lock.hcl
**/destroy*.tfplan
**/tfplan
**/*.tfplan

# 본선 자료 PDF — git 추적 X
*.pdf
PRISM_마스터가이드.pdf
# OMC state / 메모리 (로컬 only)
.omc/

# 로컬 학습 문서 — 원격 저장소에 올리지 않음
ARCHITECTURE_OVERVIEW.md
DATA_FLOW_DETAILED.md
PHASE_DEEP_DIVE.md
OPERATIONAL_GUIDE.md
DE_SKILLS_LEARNING_JOURNAL.md
프로젝트 구성될때 물리적 빈틈.md
프로젝트 정보/
requirements.txt
# 단, prism/ 배포 단위 의 requirements 와 .env.example 은 fresh-clone 부팅 위해 추적
!prism/requirements.txt
!prism/.env.example
terraform/tfplan
학습자료/
비용절감플랜/
# 로컬 AI 도구 / 세션 산출물 (공개 X)
.claude/
CLAUDE.md
# 설계/운영 문서 — 로컬 보존, GitHub에는 검토된 public 문서만 노출
docs/*
!docs/public/
!docs/public/**
# 로컬 구현 설계 — 공개 포트폴리오 문서와 분리
docs/public/ACCOUNT-PORTABILITY-DESIGN.md
# 발표 자료 (로컬 빌드 도구, git 추적 X)
presentation/
발표자료.md
PRISM_숙지자료.md
PRISM_briefing.md
PRISM_DEMO_DAY.md
PRISM_TALKING_POINTS.md
# 런타임 로그 (시연 중 stdout/stderr 누적, 결정성 무관)
logs/
